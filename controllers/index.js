const { userController } = require("./userController")
const { linkController } = require("./linksController")
const { sessionsController } = require("./sessionsController")


module.exports = {
    userController,
    linkController,
    sessionsController
}