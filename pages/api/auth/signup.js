// import db from "@/models";
// import handler from "../test-db";
// const { User } = db;

// export default async function handler(req, res) {
//     const { method, body } = req;

//     if (method === 'POST') {
//         const data = body;

//     } else {
//         res.status(405).json({ message: 'Método no permitido' })
//     }
// }